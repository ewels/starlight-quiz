---
import Default from '@astrojs/starlight/components/MobileTableOfContents.astro';
import QuizProgressBadge from '../components/QuizProgressBadge.astro';
---

<Default><slot /></Default>
{
  /* A compact counter that rides along on the fixed mobile ToC bar so it stays
    visible while scrolling. The full sidebar widget is desktop-only. */
}
<QuizProgressBadge />

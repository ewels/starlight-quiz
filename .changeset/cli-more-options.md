---
'starlight-quiz': minor
---

More CLI runner options: `run --shuffle-answers` (shuffle only the answers, leaving quiz order), `run --page <substr>` (take only the quizzes whose page path contains a string), and `history --yaml` (YAML output alongside `--json`).

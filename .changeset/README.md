# Changesets

This directory is managed by [changesets](https://github.com/changesets/changesets). Each change to the published
`starlight-quiz` package should ship with a changeset describing it. Run `pnpm changeset` to add one.

See the [common questions documentation](https://github.com/changesets/changesets/blob/main/docs/common-questions.md) for
more information.

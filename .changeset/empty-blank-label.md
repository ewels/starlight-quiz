---
'starlight-quiz': patch
---

Fill-in-the-blank corrections now show a localised `(empty)` placeholder for a blank left empty (e.g. `(empty) → Paris`), instead of an ellipsis. The string is translated in all bundled locales.
